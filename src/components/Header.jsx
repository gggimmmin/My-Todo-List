import styles from "./Header.module.css";

const Header = () => {
  return (
    <div className={styles.header}>
      <div>To-Do !</div>
      <div>React</div>
    </div>
  );
};

export default Header;
